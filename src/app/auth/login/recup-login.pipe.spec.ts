import { RecupLoginPipe } from './recup-login.pipe';

describe('RecupLoginPipe', () => {
  it('create an instance', () => {
    const pipe = new RecupLoginPipe();
    expect(pipe).toBeTruthy();
  });
});
