import { FilterCaisseJournalierePipe } from './filter-caisse-journaliere.pipe';

describe('FilterCaisseJournalierePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCaisseJournalierePipe();
    expect(pipe).toBeTruthy();
  });
});
