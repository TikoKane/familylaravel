import { ListeProformatFilterPipe } from './liste-proformat-filter.pipe';

describe('ListeProformatFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ListeProformatFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
