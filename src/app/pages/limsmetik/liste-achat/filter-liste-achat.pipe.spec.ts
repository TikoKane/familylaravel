import { FilterListeAchatPipe } from './filter-liste-achat.pipe';

describe('FilterListeAchatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListeAchatPipe();
    expect(pipe).toBeTruthy();
  });
});
