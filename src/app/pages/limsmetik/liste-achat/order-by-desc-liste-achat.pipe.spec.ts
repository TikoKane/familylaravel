import { OrderByDescListeAchatPipe } from './order-by-desc-liste-achat.pipe';

describe('OrderByDescListeAchatPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByDescListeAchatPipe();
    expect(pipe).toBeTruthy();
  });
});
