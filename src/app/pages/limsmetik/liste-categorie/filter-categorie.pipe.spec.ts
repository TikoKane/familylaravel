import { FilterCategoriePipe } from './filter-categorie.pipe';

describe('FilterCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
