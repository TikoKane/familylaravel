import { FilterRechercheCaissePipe } from './filter-recherche-caisse.pipe';

describe('FilterRechercheCaissePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRechercheCaissePipe();
    expect(pipe).toBeTruthy();
  });
});
