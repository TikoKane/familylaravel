import { FilterGerantPipe } from './filter-gerant.pipe';

describe('FilterGerantPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGerantPipe();
    expect(pipe).toBeTruthy();
  });
});
